{
  "name": "hot-take",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A transparent AI agent lab for skills, MCP plugins, approvals and traces.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "node --test tests/*.test.ts"
  }
}
